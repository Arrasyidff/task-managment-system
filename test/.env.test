# Test environment configuration
PORT=3001
NODE_ENV=test

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=task_management_test
DB_SYNC=true

# JWT
JWT_SECRET=test_secret_key
JWT_EXPIRES_IN=1d

# Redis Cache (in-memory during tests)
CACHE_TYPE=memory